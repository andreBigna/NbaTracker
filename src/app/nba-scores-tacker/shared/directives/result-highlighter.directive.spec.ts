import { ResultHighlighterDirective } from './result-highlighter.directive';

describe('ResultHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new ResultHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
